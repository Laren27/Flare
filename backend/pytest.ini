[pytest]
# Puts backend/ on sys.path so `pytest` works the same as `python -m pytest`.
# Without it only the latter finds the `app` package, which is a trap worth
# spending two lines to remove.
pythonpath = .
testpaths = tests
